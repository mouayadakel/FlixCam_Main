# Dependencies
node_modules/
/.pnp
.pnp.js

# Testing
/coverage
coverage/

# Next.js
/.next/
/out/
/build
dist/

# Misc
.DS_Store
*.pem
*.zip
*.tar.gz

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files — .env.example must NOT be ignored (keep for CI/docs)
.env
# Filled production env template (may contain API keys) — do not commit
docs/PRODUCTION_ENV_TEMPLATE.md
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma — do NOT ignore migration SQL files; they must be in repo for db:deploy / migrate deploy
app/prisma/*.db
app/prisma/*.db-journal

# IDE
.vscode/
.idea/
.history/
.cursor/
*.swp
*.swo
*~

# OS
Thumbs.db

# Logs
logs
*.log

# Temporary files
tmp/
temp/
*.tmp

# Local file storage (uploads)
storage/
uploads/
app/storage/uploads/*
!app/storage/uploads/.gitkeep

# Sentry
.sentryclirc

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/

# Lighthouse
/reports/

# Audit artifacts (generated)
docs/audit.zip

# Turbo
.turbo/

# VPS server files (not project code)
.bash_history
.gitconfig
.bash_logout
.bash_profile
.bashrc

# AWStats: password file and analytics data (secrets + PII; never commit)
.awstats-htpasswd
awstats/

# VPS deployment directory (production build + runtime, not source)
public_html/FlixCam_Main/FlixCam_Main/
